import React, { useMemo } from 'react';

// useMemo
// 이전에 연산된 값을 재사용
// 성능을 최적화해야 하는 상황에서 사용
// 특정 값이 바뀌었을 때만 특정 함수 실행하여 연산하도록 처리
// 특정 값이 바뀌지 않았다면 리랜더링 시 이전의 값을 재사용하도록 처리
function UseMemo() {
    
}
export default UseMemo;